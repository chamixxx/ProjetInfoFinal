(function() {
    'use strict';

    angular
        .module('app.examples.charts')
        .controller('ChartJsLineController', ChartJsLineController);

    ChartJsLineController.$inject=['$scope', '$log', '$q', '$http'];

    /* @ngInject */
    function ChartJsLineController($scope,$log,$q,$http) {


    }
})();